DATABASE_URL=postgresql://postgres:postgres@localhost:5432/aifactory_simulation

# sim 只服务的 canonical 工厂（md_factory.factory_code）。
# Creator 可向共享 md_factory 写入其他工厂，sim 的 GET /factories 只返回该工厂。
# 留空 = 不过滤（列出全部 ACTIVE 全局工厂）。
CANONICAL_FACTORY_CODE=FOXCONN-NME
